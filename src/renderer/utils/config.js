const adbAction = {
    add: "add",
    remove: "remove",
    end: "end",
    error: "error",
}

const adbConnect = {
    connected: "connected",
    error: "error"
}

const processAction = {
    data: "data",
    error: "error",
    close: "close",
    exit: "exit",
}

const linkType = {
    wired: "有线",
    wireless: "无线",
}

export default {
    adbAction,
    adbConnect,
    processAction,
    linkType,
}
